package com.example.pagamentos.pagamentos.view_model

import android.util.Log
import androidx.lifecycle.LiveData
import androidx.lifecycle.MutableLiveData
import androidx.lifecycle.ViewModel
import com.example.pagamentos.pagamentos.SchedulerProvider
import io.reactivex.disposables.CompositeDisposable
import javax.inject.Inject

class CPagamentosViewModel @Inject constructor(private val schedulers: SchedulerProvider) : ViewModel() {
    //TODO
}